apiVersion: v1
kind: Service
metadata:
  name: {{ include "nbc-metadata-agent.fullname" . }}
  labels:
    {{- include "nbc-metadata-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nbc-metadata-agent.selectorLabels" . | nindent 4 }}
