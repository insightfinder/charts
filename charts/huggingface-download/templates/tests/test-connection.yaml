apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "huggingface-download.fullname" . }}-test-connection"
  labels:
    {{- include "huggingface-download.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "huggingface-download.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
