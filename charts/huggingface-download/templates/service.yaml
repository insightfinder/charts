apiVersion: v1
kind: Service
metadata:
  name: {{ include "huggingface-download.fullname" . }}
  labels:
    {{- include "huggingface-download.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "huggingface-download.selectorLabels" . | nindent 4 }}
