{{- if .Values.resourceQuota.enabled }}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ printf "%s-resourcequota" (include "kafka-collector-agent.fullname" .) }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "kafka-collector-agent.labels" . | nindent 4 }}
spec:
  {{- toYaml $.Values.resourceQuota.spec | nindent 2 }}
{{- end -}}
