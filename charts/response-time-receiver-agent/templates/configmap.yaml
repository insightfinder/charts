apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "response-time-receiver-agent.fullname" . }}-config
  labels:
    {{- include "response-time-receiver-agent.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
