apiVersion: v1
kind: Service
metadata:
  name: {{ include "response-time-receiver-agent.fullname" . }}
  labels:
    {{- include "response-time-receiver-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "response-time-receiver-agent.selectorLabels" . | nindent 4 }}
