{{- if and .Values.ariJenkinsAgent.enabled .Values.ariJenkinsAgent.api.ingress.enabled }}
{{- $ariJenkinsAgentApiService := include "insightfinder.ariJenkinsAgentApiServiceName" . }}
{{ $fullname := include "insightfinder.fullname" . }}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-jenkins-api-ingress" $fullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "insightfinder.labels" . | nindent 4 }}
  annotations:
  {{- with .Values.ariJenkinsAgent.api.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ariJenkinsAgent.api.ingress.className }}
  {{- if and .Values.ariJenkinsAgent.api.ingress.hosts .Values.ariJenkinsAgent.api.ingress.tls.enabled }}
  tls:
    - hosts:
      {{- .Values.ariJenkinsAgent.api.ingress.hosts | toYaml | nindent 6 }}
      {{- with .Values.ariJenkinsAgent.api.ingress.tls.secretName }}
      secretName: {{ . }}
      {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ariJenkinsAgent.api.ingress.hosts }}
    - host: {{ . }}
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ $ariJenkinsAgentApiService }}
              port:
                number: 80
    {{- end }}
{{- end }}
