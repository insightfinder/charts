apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nbc-distrib-agent.fullname" . }}-processor-config
  labels:
    {{- include "nbc-distrib-agent.labels" . | nindent 4 }}
data:
  processor-config.yml: |
{{ tpl (.Files.Get "files/processor-config.yaml") . | indent 4 }}