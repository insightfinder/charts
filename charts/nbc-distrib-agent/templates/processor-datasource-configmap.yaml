apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nbc-distrib-agent.fullname" . }}-processor-datasources
  labels:
    {{- include "nbc-distrib-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: processor
data:
  {{- range $sourceName, $config := .Values.processor.data_sources.configs }}
  {{ $sourceName }}.yaml: |
    {{- toYaml $config | nindent 4 }}
  {{- end }}
