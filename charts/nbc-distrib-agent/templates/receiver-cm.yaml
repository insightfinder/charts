apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nbc-distrib-agent.fullname" . }}-receiver-config
  labels:
    {{- include "nbc-distrib-agent.labels" . | nindent 4 }}
data:
  receiver-config.yml: |
{{ tpl (.Files.Get "files/receiver-config.yaml") . | indent 4 }}