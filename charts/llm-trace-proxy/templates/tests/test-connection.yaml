apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-trace-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "llm-trace-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-trace-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
